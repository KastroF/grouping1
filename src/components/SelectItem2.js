import React from 'react'
import { View } from 'react-native'
import { COLORS, SIZES } from '../constants/theme'

export default function SelectItem2() {
  
  
    return (
    <View style={{
        height: SIZES.height * 0.08, 
        width: "100%", 
        borderColor: COLORS.other_blue, 
        borderWidth: 1
    }}>
        
    </View>
  )
}
